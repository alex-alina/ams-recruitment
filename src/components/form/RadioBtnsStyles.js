import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles(() => ({
  root: {
    display: 'flex',
  },
  formControl: {
    display: 'flex',
  },
  group: {
    marginTop: '8px',
    marginBottom: '20px',
  },
  genderLabel: {
    textAlign: 'left',
    marginLeft: '12px',
  }
}));